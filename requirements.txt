# Pinned to the versions the test suite passes against, not to the newest
# available - the point of a pin is to reproduce a tested environment, and
# "latest at install time" is exactly what it exists to stop.
#
# Raising one is a deliberate change with its own test run. Stale pins are their
# own risk, so this file is meant to be revisited, not left alone forever; the
# Technology and Architecture review (addendum 17 §7-§9, gateway/technology.py)
# is what notices when it has been.
anthropic==0.121.0
python-dotenv==1.2.2
# Read at runtime by app/router_config.py (config/router.yaml, Task 01 §4.3) and
# by app/model_performance.py (docs/model_registry.yaml). It was already present
# as a transitive dependency of uvicorn[standard] and pinned in
# requirements-dev.txt for a test that reads a YAML document; naming it here is
# the correction that follows from the router reading a YAML file on the live
# path. Same pin as requirements-dev.txt, deliberately - two files disagreeing
# about one package is worse than either version.
pyyaml==6.0.3
openpyxl==3.1.5
bcrypt==5.0.0
fastapi==0.141.1
uvicorn[standard]==0.52.3
requests==2.34.2
# Backup encryption (backend/continuity.py's EncryptedProvider, addendum 29
# §10, SPEC_RECONCILIATION §59): Fernet for the secondary recovery copy.
# Deferred-imported, so nothing pays for it until an encrypted destination
# is actually configured.
cryptography==50.0.0
